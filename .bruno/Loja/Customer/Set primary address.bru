meta {
  name: Set primary address
  type: http
  seq: 100
  tags: [
    bruno
    store
  ]
}

patch {
  url: {{BASE_URL}}/api/v1/me/addresses/{{ADDRESS_ID}}/primary
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  Implemented API endpoint. Replace placeholder IDs and complete the payload before execution.
}

