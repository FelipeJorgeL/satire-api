meta {
  name: Get order
  type: http
  seq: 3
  tags: [
    store
    orders
  ]
}

get {
  url: {{BASE_URL}}/api/v1/orders/{{ORDER_ID}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  Returns details, item snapshots and address snapshot for an owned order.
}
