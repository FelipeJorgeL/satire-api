meta {
  name: Cancel order
  type: http
  seq: 5
  tags: [
    store
    orders
  ]
}

post {
  url: {{BASE_URL}}/api/v1/orders/{{ORDER_ID}}/cancel
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
  Content-Type: application/json
}

body:json {
  {"reason":"Cliente desistiu da compra"}
}

docs {
  Cancels an owned order when its current status permits cancellation.
}
