meta {
  name: Remove cart item
  type: http
  seq: 100
  tags: [
    bruno
    store
  ]
}

delete {
  url: {{BASE_URL}}/api/v1/me/cart/items/{{CART_ITEM_ID}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

docs {
  Implemented API endpoint. Replace placeholder IDs and complete the payload before execution.
}

