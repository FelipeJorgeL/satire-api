meta {
  name: Get variation stock
  type: http
  seq: 100
  tags: [
    bruno
    admin
  ]
}

get {
  url: {{BASE_URL}}/api/v1/admin/inventory/variations/{{VARIATION_ID}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{ADMIN_ACCESS_TOKEN}}
}

docs {
  Implemented API endpoint. Replace placeholder IDs and complete the payload before execution.
}

